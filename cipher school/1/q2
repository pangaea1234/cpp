#include<bits/stdc++.h>

using namespace std;

int main(){
    int num;
    cin>>num;

    // if(num%2 == 0){
    //     cout<<num<<" it is a even number"<<endl;
    // }else cout<<num<<" it is a odd number"<<endl;

    // a ? b : c
    (num%2 == 0) ? cout<<num<<" it is a even num"<<endl : cout<<num<<" it is a odd num"<<endl;
    return 0;
}
