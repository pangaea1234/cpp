#include<bits/stdc++.h>

using namespace std;

int main(){
    // swap(num1,num2);
    int num1,num2;
    cin>>num1>>num2;
    num1 = num1+num2;
    num2 = num1 - num2;
    num1 = num1 - num2;
    return 0;
}
