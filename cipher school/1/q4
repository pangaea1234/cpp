#include<bits/stdc++.h>

using namespace std;

int main(){
    int num;
    cin>>num;

    int sum = 0;
    while(num > 0){
        sum = sum+(num%10);//sum+=num%10;
        num = num/10;//num/=10;
    }

    cout<<"sum of num digits is : "<<sum<<endl;

    return 0;
}
