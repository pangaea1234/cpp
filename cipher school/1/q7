#include<bits/stdc++.h>

using namespace std;

int main(){
    // pow(a,b); a raise b;
    int n,p;
    cin>>n>>p;
    int ans = 1;
    for(int i = 0 ;i<p;i++){
        ans*=n;
    }
    cout<<"n raise to the power p is: "<<ans<<endl;
    return 0;
}
