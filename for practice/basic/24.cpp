//boolean expression 
#include <bits/stdc++.h>
using namespace std;
int main() {
    int x = 10;
    int y = 9;
    cout << (x > y)<<endl; // returns 1 (true), because 10 is higher than 9
    cout << (x == 11)<<endl; // returns 0 (false), because 11 is not equal 10

    return 0;
}